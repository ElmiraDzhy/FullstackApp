import React from 'react';
import styles from './Chat.module.css';

function Chat (props) {
    return (
        <div className={styles['chat-container']}>
                (here will be chat)
        </div>
    )
}

export default Chat;